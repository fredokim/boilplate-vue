# 모듈 생성 계약

확장계획 2번의 2-7단계 산출물이다. react-boilerplate의 계약을 그대로 옮기지
않는다. 이 저장소의 관례가 실제로 다르다.

---

## 조사 — 기존 일곱 모듈은 무엇을 갖고 있나

| 모듈 | api | dto | store | views | router | stories | specs |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `auth` | 1 | 1 | – | 2 | 1 | – | 1 |
| `user` | 1 | 1 | 1 | 1 | 1 | 1 | – |
| `dashboard` | – | – | – | 1 | 1 | – | – |
| `component` | – | – | – | 4 | 1 | – | – |
| `customizable-dashboard` | – | 1 | – | 13 | 1 | – | 6 |
| `visual-graph` | – | – | – | 5 | 1 | – | 8 |
| `live-experience` | – | – | – | 5 | 1 | – | 5 |

**`router/routes.ts`는 일곱 모듈 전부가 갖는다.** react 쪽은 여섯 중 셋만
route 파일을 갖고 나머지는 라우터 파일에 직접 적혀 있는데, 여기는 예외가
없다. 라우터가 `../modules/**/router/routes.ts`를 glob으로 수집하고 모든
모듈이 그 규칙을 따른다.

`.vue` 뷰도 전부가 갖는다. 나머지는 갈린다.

**story는 일곱 중 하나뿐이다.** react에서는 여섯 기능 모두가 story를 갖는
것과 정반대다. 즉 이 저장소에서 story는 아직 관례가 아니다.

---

## 계약

### 항상 만든다

| 경로 | 이유 |
| --- | --- |
| `views/<Name>View.vue` | 일곱 모듈 모두가 갖는다 |
| `router/routes.ts` | 일곱 모듈 모두가 갖는다. 없으면 접근할 방법이 없다 |
| `__tests__/<feature>.store.spec.ts` | 아래 참고 |
| `stories/<Name>.stories.ts` | 아래 참고 |

### 함께 만든다

`dto` · `api` · `store` 셋은 함께 움직인다. 생성기는 store가 api를 호출하고
api가 dto로 응답을 검증하는 형태를 만들며, 셋 중 하나만 있는 모듈은 의미가
없다.

react 쪽에서는 이 묶음을 `--api` 플래그 뒤로 옮겼다. 생성된 api 모듈이
존재하지 않는 엔드포인트를 호출하고, 계약 테스트가 그것을 즉시 실패로
보고하기 때문이다. **이 저장소에는 계약 테스트가 없으므로** 같은 문제가
발생하지 않고, 따라서 기본으로 만든다. 계약 테스트가 생기면 이 결정을
다시 봐야 한다.

---

## 관례를 앞서가는 부분

**story는 일곱 중 하나만 갖고 있는데 생성기는 만든다.** 이것은 생성기가
저장소에 뒤처진 것이 아니라 앞서 있는 경우다. 그대로 둔다. Storybook은
`check:ci`에서 빌드되고 있고, 새 모듈이 story를 갖고 시작하는 편이 나중에
붙이는 것보다 낫다.

**spec은 넷이 갖고 있고 생성기는 만든다.** 같은 이유로 유지한다.

기존 모듈을 소급해 고치지 않는다. 계약은 앞으로 만들어질 것에 적용된다.

---

## 생성기가 이미 하고 있는 것

`scripts/generate-feature.ts`는 이 계약을 이미 만족한다. 여덟 개 파일을
만들고, 이미 존재하는 파일을 발견하면 덮어쓰지 않고 실패하며, 기능 이름이
kebab-case가 아니면 거부한다.

부족한 것은 **아무도 그것을 확인하지 않는다는 점**이었다.
`check-automation.ts`는 생성기 네 개의 파일 존재 여부만 보았고 이 생성기는
그 목록에 없었다. `scripts/check-generators.ts`가 실제로 실행해 확인한다.
